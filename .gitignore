# Local-only mockups. These contain invented provider names and prices for
# layout work; publishing them would put fabricated commercial offers on a
# live site. Never commit, never deploy.
mockup-*.html
preview-*.html
variant-*.css
probe*.html

# Exported claude.ai design reference. Loads Google Fonts (GDPR — LG München I,
# 3 O 17493/20) and carries mockup vendor data; it must never reach the live
# site. Local design reference only.
Energieausweis-Tools.standalone.html
